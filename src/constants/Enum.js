// test type
export const [multiple, mbti, weight] = ["multiple", "mbti", "weight"];

const ENUM = {
  HOME: "home",
  PICKTEST: "picktest",
  PREVIEW: "preview",
  MOVENEXT: "movenext",
  SHARE: "share",
};

export default ENUM;
