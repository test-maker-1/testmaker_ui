const colors = {};

const fontSizes = {};

const paddings = {
  main: 20
};

const widths = {
  main: 414
};

const heights = {
  header: 56,
  bottomBtn: 80
};

const theme = { colors, fontSizes, widths, heights, paddings };

export default theme;
