const colors = {
  // primary
  blue: "#0B70FD",
  skyBlue: "#ECF2FE",

  // gray scale
  white: "#FAFAFA",
  ivory: "#F1F2F4",
  ghostGray: "#EBEDF1",
  lightGray: "#CFD3DB",
  gray: "#B7BDCB",
  deepGray: "#8A929E",
  bodyGray: "#515966",
  darkGray: "#363D4A",
  black: "#1B1C1F",
};

const fontSizes = {
  // px
  xxs: 12,
  xs: 14,
  sm: 15,
  md: 16,
  lg: 18,
  xl: 20,
  xxl: 22,
  extra: 24,
};

const paddings = {
  main: 20,
};

const widths = {
  main: 414,
};

const heights = {
  header: 56,
  bottomBtn: 69,
};

const theme = { colors, fontSizes, widths, heights, paddings };

export default theme;
